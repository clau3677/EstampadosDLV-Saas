// Este layout se aplica solo a /login. Removemos el layout público/admin porque
// el LoginForm ya se auto-centra en pantalla completa.
export default function LoginLayout({ children }) {
  return children;
}
